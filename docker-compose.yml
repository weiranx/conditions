name: summitsafe

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: summitsafe-backend:latest
    container_name: summitsafe-backend
    restart: unless-stopped
    # Bind to localhost only — host nginx proxies to this port; internet can't reach it directly.
    ports:
      - "127.0.0.1:3001:3001"
    env_file:
      # Lives at /opt/summitsafe/.env on the VPS — not committed to git.
      - .env
    environment:
      NODE_ENV: production
      PORT: "3001"
      # The admin restart action sends SIGTERM to this process; the restart
      # policy above then creates a fresh backend process in the same container.
      ADMIN_BACKEND_RESTART_ENABLED: "true"
    volumes:
      # Named volume — initialized from the image's /app/data dir (owned by appuser).
      # Bind mounts would be owned by root on the host, causing EACCES for appuser.
      - report-logs-data:/app/data
      # Read-only access lets the owner-only admin endpoint display monitor
      # history without allowing the backend to modify monitor state.
      - health-monitor-data:/app/health-monitor-data:ro
    # 15s gives the existing 10s graceful-shutdown handler room to complete
    # before Docker sends SIGKILL.
    stop_grace_period: 15s
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3001/healthz"]
      interval: 30s
      timeout: 5s
      start_period: 15s
      retries: 3

  health-monitor:
    image: summitsafe-backend:latest
    container_name: summitsafe-health-monitor
    restart: unless-stopped
    command: ["node", "scripts/run-health-monitor.js"]
    env_file:
      - .env
    environment:
      NODE_ENV: production
      HEALTH_MONITOR_URL: ${HEALTH_MONITOR_URL:-http://backend:3001/healthz}
      HEALTH_ALERT_EMAIL: ${HEALTH_ALERT_EMAIL:-weiranxiong@gmail.com}
      HEALTH_MONITOR_INTERVAL_SECONDS: ${HEALTH_MONITOR_INTERVAL_SECONDS:-300}
      HEALTH_ALERT_REMINDER_SECONDS: ${HEALTH_ALERT_REMINDER_SECONDS:-21600}
    volumes:
      - health-monitor-data:/app/data
    # The image healthcheck targets the backend HTTP server. This worker has no
    # listening port; its failures are logged and restart: unless-stopped keeps
    # the monitoring loop alive independently of the backend container.
    healthcheck:
      disable: true

  postgres:
    image: postgres:17-alpine
    container_name: summitsafe-postgres
    restart: unless-stopped
    # PostgreSQL is intentionally not published on a host port. The backend
    # reaches it as `postgres:5432` over Docker's private project network.
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-summitsafe}
      POSTGRES_USER: ${POSTGRES_USER:-summitsafe_app}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-}
      POSTGRES_INITDB_ARGS: "--auth-host=scram-sha-256 --data-checksums"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    shm_size: 128mb
    stop_grace_period: 60s
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

volumes:
  health-monitor-data:
  report-logs-data:
  postgres-data:
