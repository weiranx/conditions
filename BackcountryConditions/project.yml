name: BackcountryConditions
options:
  bundleIdPrefix: com.summitsafe
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16.0"
  createIntermediateGroups: true
  groupSortPosition: top

targets:
  BackcountryConditions:
    type: application
    platform: iOS
    sources:
      - BackcountryConditions
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.summitsafe.backcountryconditions
        MARKETING_VERSION: "1.0.0"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_VERSION: "6.0"
        INFOPLIST_KEY_UIApplicationSceneManifest_Generation: YES
        INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents: YES
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad: "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone: "UIInterfaceOrientationPortrait UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        INFOPLIST_KEY_NSLocationWhenInUseUsageDescription: "Backcountry Conditions uses your location to find nearby peaks and trailheads."
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
        GENERATE_INFOPLIST_FILE: YES
        SWIFT_STRICT_CONCURRENCY: complete

  BackcountryConditionsTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - BackcountryConditionsTests
    dependencies:
      - target: BackcountryConditions
    settings:
      base:
        SWIFT_VERSION: "6.0"
